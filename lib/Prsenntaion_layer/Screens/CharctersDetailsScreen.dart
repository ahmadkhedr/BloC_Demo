import 'package:flutter/material.dart';

class ChaectersDetailsScreen extends StatelessWidget {
  const ChaectersDetailsScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}